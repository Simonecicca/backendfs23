package com.lezioneepicode;

public class Main_Project {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}
	
	boolean b = true; //true/false;
	char c = 'c'; //apici singoli, un carattere
	int i = 25; //valore numerico non decimale, senza virgola
	double d = 25.5; //valore numerico devimale
	
	String s = "ciao"; //apici doppi, una stringa
	
	int val1 = 10; // Dichiarare e inizializzare una variabile
	int val2; //Dichiarato una variabile
	val2 = 10; //Inizializzato una variabile
	final int VAL3 = 15; //Definisco ed Inizializzo una costante
	
	int [] arrNum = new int[5]; //creo un array di valori numerici dio dimensione fissa (5) con indice da 0 a n-1
	arrNum[0] = 25; // inizializzo un valore nell'indice 0 del mio array
	arrNum[1] = 10; //inizializzo un valore nell'indice 1 del mio array
	System.out.println(arrNum[1]); //Questo è come il console.log() di Java Script
	String[] arrString = new String[3]; // creo un array di valori String di dimensione fissa (3) con indice da 0 a n-1
	
	char[] arrChar = {'a', 'b', 'c'}; //creo un array e lo popolo con dei valori
	
	for (int j=0; j<arrChar.length; j++) {
		System.out.println(arrChar[j]);
		}
	 int num1 = 25; //implicito
	 int num2 = num1;
	 long num3 = num1; //un valore più piccolo entra in uno più grande
	 int num4 = num3; // un valore più grande non può entrare in uno più piccolo
	 int num4 = (int) num3; //posso inserire un valore più grande in uno più piccolo SE io lo specifico e mi "assumo" la responsabilità
	 long num3 = 193;
	 double num5 = num4; //193.0
	 int num6 = (int) num5; //193 (esplicito)
	 
	 //let c = 10/3; //3.33333 in Java Script da il decimale
	 int cj = 10/3; //3
	 int resto = 10%3; //1
	 
	 num1 = num1 + 5;
	 num1 += 5;
	 
	  boolean cond1 = 5 > 0 && 5 < 10; //true && true -> true
	  boolean cond2 = 5 > 10 && 5 < 10; //false
	  boolean cond3 = 5 > 10 || 5 < 10; // true OR (||) se UNA delle due condizioni è true, allora ritorna true
	  boolean con4 = (5 > 10); // false con con il not -> true (!)
	  boolean cond5 = 5 > 10 ^ 5 < 10; // true && false -> true -> per essere vero solo una delle due condizioni deve essere vera, altrimenti in ogni altro caso è false
	 
	  
	  int res = somma(25, 10);
	  System.out.println(res);
	  
	 
	

	public static int somma(int x, int y) {
	  int result = x + y;						//metodo con valore di ritorno
	  return result;
	}
	
	public static void stampaSomma (int x, int y) {
		int result = x + y;								//metodo senza valore di ritorno
		System.out.println(result);
	}
	
	}

}
